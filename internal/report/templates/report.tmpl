<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Individual Report {{.Year}}</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 20px;
      background: white;
      color: #000;
    }
    .container {
      max-width: 1200px;
      margin: 0 auto;
    }
    h1 {
      background: #4472C4;
      color: white;
      text-align: center;
      padding: 15px;
      margin: 0 0 20px 0;
      font-size: 24px;
      font-weight: bold;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 30px;
    }
    th, td {
      border: 1px solid #000;
      padding: 10px;
      text-align: left;
      vertical-align: top;
    }
    th {
      background: #FFC000;
      color: #000;
      font-weight: bold;
      text-align: center;
    }
    .header-row td {
      background: #FFC000;
      font-weight: bold;
    }
    .header-label {
      width: 10%;
    }
    .summary {
      margin-top: 20px;
      font-size: 14px;
    }
    .summary-item {
      margin: 10px 0;
    }
    a {
      color: #0066CC;
      text-decoration: none;
    }
    a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>INDIVIDUAL REPORT {{.Year}}</h1>

    <table>
      <tr class="header-row">
        <td class="header-label">Dept:</td>
        <td colspan="3">{{.Department}}</td>
        <td class="header-label">Submitted by:</td>
        <td colspan="3">{{.SubmittedBy}}</td>
      </tr>
      <tr class="header-row">
        <td class="header-label">PERIOD</td>
        <td colspan="7">{{.Period}}</td>
      </tr>
      <tr>
        <th>KEY ACTIVITIES / TASKS</th>
        <th>ACHIEVEMENTS</th>
        <th>CHALLENGES ENCOUNTERED</th>
        <th>SUPPORT REQUIRED</th>
        <th>SUPPORT FROM (WHOM/ WHICH DEPT)</th>
        <th>FOLLOW UP ACTIVITIES</th>
        <th>COMPLETION DATE</th>
        <th>LOCATION OF EVIDENCE / ATTACHMENT</th>
      </tr>
      {{range .Tasks}}
      <tr>
        <td><strong>{{.Title}}</strong></td>
        <td>{{if .Achievements}}{{.Achievements}}{{else}}&nbsp;{{end}}</td>
        <td>{{if .Challenges}}{{.Challenges}}{{else}}&nbsp;{{end}}</td>
        <td>{{if .SupportRequired}}{{.SupportRequired}}{{else}}&nbsp;{{end}}</td>
        <td>{{if .SupportFrom}}{{.SupportFrom}}{{else}}&nbsp;{{end}}</td>
        <td>{{if .FollowUp}}{{.FollowUp}}{{else}}&nbsp;{{end}}</td>
        <td>{{if .CompletedAt}}{{.CompletedAt.Format "2006-01-02"}}{{else}}&nbsp;{{end}}</td>
        <td>
          {{if .AttachmentURL}}
            <a href="{{.AttachmentURL}}" target="_blank">Open Attachment</a>
          {{else}}
            &nbsp;
          {{end}}
        </td>
      </tr>
      {{end}}
    </table>

    <div class="summary">
      <h3>Activities Summary</h3>
      <div class="summary-item"><strong>Total Activities:</strong> {{index .Stats "total"}}</div>
      <div class="summary-item"><strong>Completed:</strong> {{index .Stats "completed"}}</div>
      <div class="summary-item"><strong>Pending:</strong> {{sub (index .Stats "total") (index .Stats "completed")}}</div>
      
      <h3>By Source</h3>
      {{range $src, $cnt := index .Stats "by_source"}}
      <div class="summary-item"><strong>{{title $src}}:</strong> {{$cnt}}</div>
      {{end}}
      
      <h3>By Status</h3>
      {{range $st, $cnt := index .Stats "by_status"}}
      <div class="summary-item"><strong>{{title $st}}:</strong> {{$cnt}}</div>
      {{end}}
    </div>
  </div>
</body>
</html>

